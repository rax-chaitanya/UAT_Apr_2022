@isTest

public class HighLowMarginControllerTest {
    static testMethod void testConvertToWrappperList(){
        Test.startTest();
        new HighLowMarginController().CoverageModule3();
        system.assert(true, 'Running as expected');
        Test.stopTest();        
    }
}